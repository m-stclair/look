{
  "name": "look-minimal",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node tools/check-js.js",
    "test": "node --test tests/*.test.js",
    "test:all": "npm run check && npm test"
  }
}
